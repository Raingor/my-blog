# 角色设定
你现在是一位资深的 AI 技能工程师（Skill Engineer）。你的任务是根据我们刚才这个项目的完整对话历史，反向工程出一个可复用的“Skill”。

# 目标
我要把我们刚才完成的 [yescialis.com] 的流程标准化。请分析我们刚才的交互过程，提取出核心的工作流、规则和模板，将其封装成一个标准的 Skill 包。

# 执行步骤
请按照以下步骤思考并输出：

1. **分析成功路径**：回顾我们刚才的任务，找出最关键的执行步骤（例如：先做首页，提取配色，再做内页）。
2. **提取隐性规则**：找出我在对话中反复强调的偏好（例如：使用 Tailwind CSS，代码风格要模块化，必须先写 API 文档等）。
3. **定义触发器**：思考未来用户在什么场景下会用到这个 Skill，应该说什么话触发它。
4. **生成文件结构**：请为我生成一个标准的 Skill 文件夹结构和代码。

# 输出要求
请严格按照以下格式输出（假设是基于 Markdown 的 Skill 格式）：

1. **SKILL.md 内容**：
   - 包含 `name`（技能名称，用 kebab-case，如 `web-dev-pro`）。
   - 包含 `description`（清晰描述技能用途和触发条件）。
   - 包含 `instructions`（核心指令，告诉 AI 拿到任务后第一步做什么、第二步做什么，如何处理边界情况）。
2. **资源文件（如有）**：如果我们在项目中使用了特定的模板或代码规范，请将其提取为单独的 `.md` 或 `.txt` 文件内容，并在 SKILL.md 中注明引用路径。

# 限制
- 技能描述要具体，不要笼统。
- 指令要步骤化，具有可执行性。
- 确保生成的 Skill 可以“即插即用”，不需要我再做复杂修改。

现在，请开始分析并生成我的 Skill。
skills 目录：/Users/a123/workspace/wwwroot/my-skills