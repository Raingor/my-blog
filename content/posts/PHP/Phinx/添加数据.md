# 添加数据

```bash
 $table = $this->table('ctmon_config');
        $table->insert([
            'name' => 'GoogleTagsId',
            'code' => 'google_tags_id',
            'type' => 'text',
            'value' => "",
            'tip' => '谷歌标签管理器',
            "create_time" => time(),
            "update_time" => time()
        ])->saveData();
```